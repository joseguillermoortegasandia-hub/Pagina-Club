import { requireSession } from '@/lib/auth';
import { AccountClient } from '@/components/AccountClient';
export const metadata={title:'Mi cuenta'};
export default async function AccountPage(){const {profile,club,isDemo}=await requireSession();return <div className="page"><div className="page-head"><div className="page-title"><h1>♙ Mi Cuenta</h1><p>Datos del socio, seguridad y preferencias.</p></div></div><div className="account-grid"><section className="card profile-card"><span className="avatar">{profile.full_name.slice(0,1)}</span><h2>{profile.full_name}</h2><p className="muted">Acción #{profile.action_number}</p><span className="tag">{profile.role}</span><hr style={{border:0,borderTop:'1px solid #e4eaf0',margin:'22px 0'}}/><p><b>{club.name}</b></p><p className="muted">Estado: {profile.status||'Activo'}</p><p className="muted">{profile.notification_email}</p></section><section className="card"><div className="section-head"><h2>Configuración</h2></div><AccountClient demo={isDemo}/></section></div></div>}
